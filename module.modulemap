framework module TFYSwiftSSRKit {
    umbrella header "TFYSwiftSSRKit.h"
    
    export *
    module * { export * }
    
    // 明确排除C库头文件，避免警告
    explicit module Private {
        // 这些模块不会被公开，但会在内部使用
        header "shadowsocks-rust/output/include/ss.h"
        export *
    }
} 